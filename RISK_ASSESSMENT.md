# تقرير دراسة المخاطر وتشخيص الأخطاء - مشروع اللجنة الطبية (MMC)

## 1. تحليل الأخطاء الحالية (Diagnosis)

### أ. خطأ البناء في الواجهة الأمامية (Frontend Build Error)
*   **المشكلة:** فشل عملية البناء على Vercel بسبب عدم القدرة على تحديد مسار الملف `./lib/api/client` من داخل `src/core/notification-engine.js`.
*   **السبب الجذري:** تعارض في هيكلية المسارات؛ الملف موجود في `src/lib/api/client.js` بينما الاستدعاء يتم بمسار نسبي غير دقيق أو يعتمد على إعدادات `alias` غير مفعلة بشكل صحيح في سياق معين.
*   **التأثير:** توقف كامل لعملية النشر (Deployment Blocked).

### ب. هيكلية الـ API والمزامنة (Backend/API Sync)
*   **المشكلة:** وجود مشروعين منفصلين (`love` و `love-api`) مع تداخل في المسؤوليات. الـ Frontend يحاول الاتصال بـ `/api/v1/*` التي قد لا تكون متوفرة إذا لم يتم إعداد البروكسي أو النطاق بشكل صحيح.
*   **السبب الجذري:** الـ Frontend يعتمد على `VITE_API_BASE_URL` بينما الـ Backend (love-api) مصمم للعمل كخادم مستقل (Node.js/PM2) أو كـ Edge Functions.
*   **التأثير:** فشل طلبات تسجيل الدخول، استعلامات الطوابير، والتحقق من الـ PIN.

### ج. أخطاء منطقية ووظيفية (Logical & Functional Errors)
*   **الاستيراد غير الآمن:** استخدام `supabase` مباشرة في `notification-engine.js` (السطر 16) دون استيراد العميل، مما يسبب `ReferenceError`.
*   **تكرار المنطق:** وجود منطق معالجة الطوابير في كل من `v1.js` (Backend) وبعض ملفات الـ Frontend، مما قد يؤدي لتضارب البيانات.

---

## 2. دراسة المخاطر (Risk Assessment)

| المخاطرة | المستوى | التأثير | خطة التخفيف (Mitigation) |
| :--- | :--- | :--- | :--- |
| **فشل الاتصال بـ Supabase** | مرتفع | توقف التطبيق بالكامل للمراجعين والإدارة | التأكد من صحة مفاتيح `service_role` و `anon` في بيئة Vercel و Supabase. |
| **توقف الـ Edge Functions** | متوسط | فشل العمليات الحساسة (توليد PIN) | توحيد المسارات واستخدام `love-api` كمرجع أساسي للعمليات المنطقية. |
| **تعارض النسخ (Version Mismatch)** | متوسط | ظهور بيانات قديمة أو أخطاء في واجهة المستخدم | تفعيل نظام `Cache-Control: no-store` وضمان توافق الـ Schema بين الكود وقاعدة البيانات. |
| **كسر الهوية البصرية** | منخفض | تجربة مستخدم سيئة | الالتزام بالثيم الطبي الأصلي وعدم تعديل ملفات CSS إلا للضرورة التقنية. |

---

## 3. خطة الإصلاح المقترحة (Action Plan)

1.  **إصلاح المسارات:** تعديل الاستيراد في `notification-engine.js` ليكون `@/lib/api/client` لضمان عمله في جميع البيئات.
2.  **تصحيح الكود:** إضافة استيراد `supabase` المفقود أو استخدام `apiClient` الموحد.
3.  **توحيد النشر:** 
    *   ضبط إعدادات Vercel لتوجيه طلبات `/api/*` إلى `love-api` أو استخدام النطاق المباشر.
    *   تحديث ملفات بيئة العمل (ENV) بالمفاتيح المزودة.
4.  **الاختبار الحي:** إجراء اختبارات شاملة على `mmc-mms.com` للتأكد من عمل شاشات المراجعين والإدارة بنسبة 100%.

> **ملاحظة:** سيتم البدء فوراً بتنفيذ الإصلاحات البرمجية في المرحلة القادمة.

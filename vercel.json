{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "npm run build || yarn build || pnpm build",
  "outputDirectory": ".vercel/output",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD package.json yarn.lock pnpm-lock.yaml || echo 'changed'",
  "env": { "NODE_ENV": "production" },
  "functions": {
    "api/v1/[...path].js": { "memory": 1024, "maxDuration": 10 }
  }
}

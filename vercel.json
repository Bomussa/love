{
  "buildCommand": "npm run build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "routes": [
    { "src": "/api/.*", "status": 404 },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}

[extend]
useDefault = true

[allowlist]
description = "Allow placeholder values and docs examples"
regexes = [
  '''<SUPABASE_[A-Z0-9_]+>''',
  '''<VERCEL_[A-Z0-9_]+>''',
  '''<GITHUB_[A-Z0-9_]+>'''
]

{
  "name": "mmc-mms-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Medical Committee Integrated Application monorepo (frontend + api proxy).",
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "tsc",
    "preview": "npm run preview --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "test": "npm run test --workspace frontend",
    "typecheck": "tsc --noEmit",
    "test:smoke": "node --test scripts/smoke.test.ts",
    "ci:resilient": "./scripts/resilient-ci.sh",
    "release:one-screen": "bash ./scripts/one-screen-release.sh",
    "check:migrations": "bash scripts/ci/check-migration-naming.sh",
    "test:integration:unified": "node scripts/ci/run-unified-integration.mjs",
    "coverage:unified": "node scripts/ci/build-unified-coverage.mjs"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^7.2.1"
  }
}

{
  "name": "mmc-mms-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Medical Committee Integrated Application monorepo (frontend + api proxy).",
  "scripts": {
    "autopilot:validate": "node scripts/validate-autopilot.js",
    "autopilot:route": "node scripts/autopilot-router.js",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  },
  "engines": {
    "node": "20.x"
  }
}

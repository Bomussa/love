# 📋 الخطة الهندسية الشاملة - مشروع اللجنة الطبية

**تاريخ الإعداد**: 25 أكتوبر 2025  
**المهندس المسؤول**: مهندس برمجيات رئيسي  
**الحالة**: 🟡 **قيد التخطيط - في انتظار الموافقة**

---

## 🎯 الهدف الرئيسي

نقل Backend بالكامل من Cloudflare KV إلى Supabase PostgreSQL مع ضمان:
- ✅ عدم حدوث أي أخطاء أو انقطاع في الخدمة
- ✅ الحفاظ على جميع الوظائف الحالية
- ✅ عدم تغيير الهوية البصرية
- ✅ تحسين الأداء والاستقرار
- ✅ الاختبار الشامل قبل كل خطوة

---

## 📊 الوضع الحالي (نتائج الفحص)

### البنية الحالية
```
love/
├── api/v1/              ← Backend Endpoints (21 ملف)
│   ├── admin/          (1 endpoint)
│   ├── clinic/         (1 endpoint)
│   ├── events/         (1 endpoint)
│   ├── path/           (1 endpoint)
│   ├── patient/        (1 endpoint)
│   ├── pin/            (3 endpoints)
│   ├── queue/          (4 endpoints)
│   ├── reports/        (4 endpoints)
│   ├── route/          (2 endpoints)
│   ├── stats/          (2 endpoints)
│   └── status.js       (1 endpoint)
├── functions/lib/       ← Supabase Client (جاهز لكن غير مستخدم)
├── src/                 ← Frontend (React)
└── diagnostics/         ← التقارير والتحليلات
```

### الإحصائيات
- **إجمالي Endpoints**: 21
- **Endpoints تستخدم KV**: 14 (67%)
- **Endpoints بدون KV**: 7 (33%)
- **إجمالي استدعاءات KV**: 43
- **نسبة الترحيل الفعلية**: 0%

### قاعدة البيانات الجديدة
- **Supabase URL**: https://rujwuruuosffcxazymit.supabase.co
- **الحالة**: جاهزة ومتصلة ✅
- **الجداول المطلوبة**: 9 جداول
- **Schema SQL**: موجود وجاهز للتنفيذ

---

## 🗺️ خريطة الطريق (Roadmap)

### المرحلة 1: إعداد البنية التحتية ⏱️ 2-3 ساعات

#### 1.1 إعداد قاعدة البيانات
- [ ] تنفيذ Schema SQL في Supabase Dashboard
- [ ] إنشاء الجداول التسعة
- [ ] إضافة Indexes للأداء
- [ ] تفعيل Row Level Security (RLS)
- [ ] اختبار الاتصال والـ CRUD

#### 1.2 إعداد Supabase Client
- [ ] نقل `functions/lib/supabase.js` إلى `api/lib/supabase.js`
- [ ] إضافة helper functions إضافية
- [ ] اختبار جميع الوظائف
- [ ] توثيق الاستخدام

#### 1.3 إعداد Environment Variables
- [ ] إضافة `SUPABASE_URL` في Vercel
- [ ] إضافة `SUPABASE_ANON_KEY` في Vercel
- [ ] التطبيق على Production + Preview + Development
- [ ] اختبار الوصول للمتغيرات

---

### المرحلة 2: ترحيل Core Queue System ⏱️ 4-6 ساعات

**الأولوية القصوى - النظام الأساسي للطوابير**

#### 2.1 queue/enter.js (3 KV calls)
**الوظيفة**: إضافة مريض للطابور
**التحويل المطلوب**:
```javascript
// القديم
await env.KV_QUEUES.get(`queue:list:${clinic}`)
await env.KV_QUEUES.put(queueKey, JSON.stringify(queueData))
await env.KV_QUEUES.put(`queue:user:${clinic}:${user}`)

// الجديد
await supabase.from('queue').select('*').eq('clinic_id', clinic)
await supabase.from('queue').insert(entry)
```

**الاختبارات المطلوبة**:
- [ ] إضافة مريض جديد
- [ ] منع التكرار (مريض موجود بالفعل)
- [ ] حساب الموقع الصحيح
- [ ] التعامل مع الأخطاء

#### 2.2 queue/call.js (3 KV calls)
**الوظيفة**: استدعاء المريض التالي
**التحويل المطلوب**:
```javascript
// القديم
await env.KV_QUEUES.get(`queue:list:${clinic}`)
await env.KV_QUEUES.get(`queue:user:${clinic}:${user}`)
await env.KV_QUEUES.put(queueKey, JSON.stringify(updatedQueue))

// الجديد
await supabase.from('queue')
  .select('*')
  .eq('clinic_id', clinic)
  .eq('status', 'waiting')
  .order('position', { ascending: true })
  .limit(1)
await supabase.from('queue').update({ status: 'called' })
```

**الاختبارات المطلوبة**:
- [ ] استدعاء المريض الصحيح
- [ ] تحديث الحالة
- [ ] إرسال إشعار
- [ ] التعامل مع طابور فارغ

#### 2.3 queue/done.js (5 KV calls)
**الوظيفة**: إنهاء فحص المريض
**التحويل المطلوب**:
```javascript
// القديم
5 استدعاءات KV لتحديث الحالة وحذف السجلات

// الجديد
await supabase.from('queue')
  .update({ 
    status: 'completed',
    completed_at: new Date().toISOString()
  })
  .eq('patient_id', patientId)
```

**الاختبارات المطلوبة**:
- [ ] إنهاء الفحص بنجاح
- [ ] تحديث الإحصائيات
- [ ] استدعاء المريض التالي تلقائياً
- [ ] التعامل مع الأخطاء

#### 2.4 queue/status.js (2 KV calls)
**الوظيفة**: الحصول على حالة الطابور
**التحويل المطلوب**:
```javascript
// القديم
await env.KV_QUEUES.get(`queue:list:${clinic}`)
await env.KV_QUEUES.get(`queue:user:${clinic}:${user}`)

// الجديد
await supabase.from('queue')
  .select('*')
  .eq('clinic_id', clinic)
  .in('status', ['waiting', 'called'])
```

**الاختبارات المطلوبة**:
- [ ] الحصول على حالة الطابور
- [ ] حساب عدد المنتظرين
- [ ] حساب موقع المريض
- [ ] الأداء مع بيانات كثيرة

---

### المرحلة 3: ترحيل Real-time & Admin ⏱️ 3-4 ساعات

#### 3.1 events/stream.js (5 KV calls)
**الوظيفة**: بث الأحداث في الوقت الفعلي
**التحويل المطلوب**:
```javascript
// القديم
5 استدعاءات KV لقراءة البيانات

// الجديد
await supabase.from('queue').select('*')
// + Supabase Realtime Subscriptions
```

**الاختبارات المطلوبة**:
- [ ] بث الأحداث
- [ ] التحديثات الفورية
- [ ] الأداء
- [ ] معالجة الأخطاء

#### 3.2 admin/status.js (3 KV calls)
**الوظيفة**: لوحة تحكم الإدارة
**التحويل المطلوب**:
```javascript
// القديم
await env.KV_PINS.get()
await env.KV_QUEUES.get() (2 مرات)

// الجديد
await supabase.from('clinics').select('pin_code, pin_expires_at')
await supabase.from('queue').select('*')
```

**الاختبارات المطلوبة**:
- [ ] عرض الإحصائيات
- [ ] عرض الطوابير
- [ ] عرض PINs
- [ ] الأداء

---

### المرحلة 4: ترحيل Endpoints المتوسطة ⏱️ 3-4 ساعات

#### 4.1 clinic/exit.js (2 KV)
- تحديث حالة العيادة عند الخروج

#### 4.2 path/choose.js (2 KV)
- اختيار المسار الديناميكي للمريض

#### 4.3 pin/status.js (2 KV)
- الحصول على حالة PIN

#### 4.4 patient/login.js (1 KV)
- تسجيل دخول المريض

---

### المرحلة 5: ترحيل Endpoints المتبقية ⏱️ 2-3 ساعات

#### 5.1 pin/generate.js (1 KV)
- توليد PIN جديد

#### 5.2 pin/verify.js (1 KV)
- التحقق من PIN

#### 5.3 route/create.js (1 KV)
- إنشاء مسار جديد

#### 5.4 route/get.js (1 KV)
- الحصول على المسارات

---

### المرحلة 6: الاختبار الشامل ⏱️ 4-6 ساعات

#### 6.1 اختبار الوحدات (Unit Testing)
- [ ] اختبار كل endpoint منفرداً
- [ ] اختبار جميع السيناريوهات
- [ ] اختبار حالات الأخطاء
- [ ] اختبار الأداء

#### 6.2 اختبار التكامل (Integration Testing)
- [ ] اختبار التدفق الكامل للمريض
- [ ] اختبار التدفق الكامل للإدارة
- [ ] اختبار Real-time Updates
- [ ] اختبار تحت الضغط

#### 6.3 اختبار الأمان (Security Testing)
- [ ] اختبار Row Level Security
- [ ] اختبار الصلاحيات
- [ ] اختبار SQL Injection
- [ ] اختبار الجلسات

#### 6.4 اختبار الأداء (Performance Testing)
- [ ] قياس زمن الاستجابة
- [ ] اختبار مع 100 مريض
- [ ] اختبار مع 1000 مريض
- [ ] مقارنة مع KV

---

### المرحلة 7: النشر التدريجي ⏱️ 2-3 ساعات

#### 7.1 النشر على Preview
- [ ] نشر على بيئة Preview
- [ ] اختبار شامل
- [ ] مراقبة الأخطاء
- [ ] جمع التغذية الراجعة

#### 7.2 النشر على Production
- [ ] نشر على Production
- [ ] مراقبة فورية
- [ ] الاستعداد للتراجع
- [ ] توثيق النتائج

#### 7.3 المراقبة والتحقق
- [ ] مراقبة لمدة 24 ساعة
- [ ] التحقق من جميع الوظائف
- [ ] معالجة أي مشاكل
- [ ] تحديث التوثيق

---

## 📋 قائمة المراجعة (Checklist)

### قبل البدء
- [ ] قراءة وفهم الخطة بالكامل
- [ ] التأكد من وجود نسخة احتياطية من KV
- [ ] التأكد من إعدادات Vercel
- [ ] التأكد من إعدادات Supabase
- [ ] إعداد بيئة الاختبار

### أثناء التنفيذ
- [ ] اتباع الترتيب المحدد
- [ ] اختبار كل endpoint بعد الترحيل
- [ ] توثيق أي مشاكل
- [ ] عدم الانتقال للخطوة التالية قبل النجاح
- [ ] الاحتفاظ بسجل التغييرات

### بعد الانتهاء
- [ ] اختبار شامل للنظام بالكامل
- [ ] التحقق من جميع الوظائف
- [ ] مراجعة الأداء
- [ ] تحديث التوثيق
- [ ] أرشفة الكود القديم

---

## ⚠️ المخاطر والتخفيف

### المخاطر المحتملة

1. **فقدان البيانات**
   - **الاحتمالية**: منخفضة
   - **التأثير**: حرج
   - **التخفيف**: نسخ احتياطي كامل قبل البدء

2. **انقطاع الخدمة**
   - **الاحتمالية**: متوسطة
   - **التأثير**: عالي
   - **التخفيف**: نشر تدريجي + خطة تراجع

3. **مشاكل في الأداء**
   - **الاحتمالية**: منخفضة
   - **التأثير**: متوسط
   - **التخفيف**: اختبار الأداء + Indexes

4. **أخطاء في المنطق**
   - **الاحتمالية**: متوسطة
   - **التأثير**: عالي
   - **التخفيف**: اختبار شامل + مراجعة الكود

---

## 📊 مؤشرات النجاح (KPIs)

### المؤشرات التقنية
- ✅ 100% من Endpoints تعمل بدون أخطاء
- ✅ زمن استجابة أقل من 200ms
- ✅ 0 أخطاء في Production
- ✅ 100% من الاختبارات تنجح

### المؤشرات الوظيفية
- ✅ جميع الوظائف تعمل كما كانت
- ✅ لا توجد شكاوى من المستخدمين
- ✅ الهوية البصرية لم تتغير
- ✅ التجربة أفضل أو مماثلة

---

## 🕐 الجدول الزمني المتوقع

| المرحلة | المدة المتوقعة | الحالة |
|---------|----------------|--------|
| 1. البنية التحتية | 2-3 ساعات | ⏳ قيد الانتظار |
| 2. Core Queue System | 4-6 ساعات | ⏳ قيد الانتظار |
| 3. Real-time & Admin | 3-4 ساعات | ⏳ قيد الانتظار |
| 4. Endpoints المتوسطة | 3-4 ساعات | ⏳ قيد الانتظار |
| 5. Endpoints المتبقية | 2-3 ساعات | ⏳ قيد الانتظار |
| 6. الاختبار الشامل | 4-6 ساعات | ⏳ قيد الانتظار |
| 7. النشر والمراقبة | 2-3 ساعات | ⏳ قيد الانتظار |
| **المجموع** | **20-29 ساعة** | **3-4 أيام عمل** |

---

## 🎯 التوصيات النهائية

### قبل الموافقة على البدء
1. ✅ مراجعة هذه الخطة بالكامل
2. ✅ التأكد من فهم جميع المراحل
3. ✅ التأكد من توفر الوقت الكافي
4. ✅ التأكد من جاهزية جميع الأدوات
5. ✅ إعداد خطة الطوارئ

### أثناء التنفيذ
1. ⚠️ عدم التسرع في أي خطوة
2. ⚠️ الاختبار بعد كل تعديل
3. ⚠️ التوثيق المستمر
4. ⚠️ المراقبة الدقيقة
5. ⚠️ الاستعداد للتراجع

### بعد الانتهاء
1. 📊 مراجعة النتائج
2. 📊 جمع التغذية الراجعة
3. 📊 تحديث التوثيق
4. 📊 أرشفة الكود القديم
5. 📊 الاحتفال بالنجاح! 🎉

---

**الحالة الحالية**: 🟡 **في انتظار موافقة المستخدم للبدء**

**الخطوة التالية**: إعداد تقرير الفحص الشامل `/diagnostics/full-inspection.json`


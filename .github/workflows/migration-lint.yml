name: Migration Lint

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]

jobs:
  migration-naming:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate migration naming convention
        run: bash scripts/ci/check-migration-naming.sh

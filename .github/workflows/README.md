# GitHub Actions Temporarily Disabled

تم تعطيل جميع workflows مؤقتاً حتى يتم التحقق من اتصال الواجهة الأمامية بالخلفية.

الملفات المعطلة موجودة في: `.github/workflows-disabled/`

## لإعادة التفعيل:
```bash
mv .github/workflows-disabled/*.yml .github/workflows/
```

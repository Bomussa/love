# ضع هنا رابط الباك-إند المباشر (محلياً أو إنتاج/اختبار)
# أمثلة:
# VITE_API_BASE=http://localhost:3000
# VITE_API_BASE=https://api.your-domain.com
VITE_API_BASE=http://localhost:3000

# عند التطوير: انسخّه للملف الفعلي
# cp .env.local.example .env.local

# ===== Live Drill E2E Testing (Optional) =====
# Configuration for live drill simulation tests
# See docs/live-drill.md for setup instructions

# Base URL for drill tests (usually same as VITE_API_BASE)
# BASE_URL=http://localhost:5173

# Drill clinic configuration (must match clinic created in admin dashboard)
# DRILL_CLINIC_ID=DRILL
# DRILL_CLINIC_PIN=9999
# DRILL_EXAM_TYPE=تجنيد
# DRILL_GENDER=male

# Optional admin credentials for admin flow tests
# ADMIN_COOKIE=session=your-session-cookie
# ADMIN_AUTH_HEADER=Bearer your-auth-token

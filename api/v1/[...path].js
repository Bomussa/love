/**
 * Catch-all API Router for /api/v1/*
 * Handles all requests to /api/v1/anything
 */

// Import the main handler
import handler from '../index.js';

// Export the handler
export default handler;

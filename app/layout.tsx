export const metadata = { title: 'Love' };
export default function RootLayout({ children }) {
  return (<html lang="ar" dir="rtl"><body style={{margin:0,fontFamily:'system-ui'}}>{children}</body></html>);
}

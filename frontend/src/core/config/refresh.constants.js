/**
 * Refresh Constants - ثوابت التحديث
 * محسّنة للتحديث اللحظي الحقيقي
 */

// فترة التحديث العامة (1 ثانية)
export const GENERAL_REFRESH_INTERVAL = 1000;

// فترة تحديث الطوابير (1 ثانية)
export const QUEUE_REFRESH_INTERVAL = 1000;

// فترة تحديث الإشعارات (1 ثانية)
export const NOTIFICATION_REFRESH_INTERVAL = 1000;

// فترة تحديث الإحصائيات (1 ثانية)
export const STATS_REFRESH_INTERVAL = 1000;

// فترة تحديث PIN (1 ثانية)
export const PIN_REFRESH_INTERVAL = 1000;

// فترة التحديث اللحظي (1 ثانية)
export const REALTIME_REFRESH_INTERVAL = 1000;

// فترة تحديث اقتراب الدور (1 ثانية)
export const NEAR_TURN_REFRESH_INTERVAL = 1000;

// فترة تحديث شاشة العرض (1 ثانية)
export const DISPLAY_REFRESH_INTERVAL = 1000;

// فترة تحديث لوحة الإدارة (1 ثانية)
export const ADMIN_REFRESH_INTERVAL = 1000;

export default {
  GENERAL_REFRESH_INTERVAL,
  QUEUE_REFRESH_INTERVAL,
  NOTIFICATION_REFRESH_INTERVAL,
  STATS_REFRESH_INTERVAL,
  PIN_REFRESH_INTERVAL,
  REALTIME_REFRESH_INTERVAL,
  NEAR_TURN_REFRESH_INTERVAL,
  DISPLAY_REFRESH_INTERVAL,
  ADMIN_REFRESH_INTERVAL,
};
# توثيق شامل لمشروع Love بعد إعادة الهيكلة

**التاريخ:** 6 نوفمبر 2025  
**المؤلف:** Manus AI  
**الهدف:** توثيق هيكل المشروع بعد إزالة الملفات غير المستخدمة وتطبيق Local Storage كبديل كامل للـ Backend.

---

## 1. ملخص البنية المعمارية

يعتمد المشروع حالياً على بنية **Single Page Application (SPA)** باستخدام **React + Vite**، حيث تم استبدال جميع خدمات الـ Backend (Supabase) بـ **Local Storage** لغرض الاختبار والتشغيل المحلي.

| المكون | التقنية | الدور |
|--------|---------|-------|
| **الواجهة الأمامية** | React 19 + Vite | عرض الواجهة، إدارة الحالة، التفاعل مع API |
| **الـ Backend** | Local Storage | محاكاة قاعدة البيانات، حفظ بيانات الطوابير، PINs، الإشعارات، والتقارير |
| **التوجيه** | Vercel (SPA) | توجيه جميع المسارات إلى `index.html` |

---

## 2. هيكل المشروع بعد إعادة الهيكلة

تم نقل جميع الملفات غير المستخدمة (المرتبطة بـ Supabase و API القديم) إلى مجلد `frontend/src/_archive`.

### 📁 `frontend/src/` (الملفات والمجلدات الرئيسية)

| المسار | الوصف |
|--------|--------|
| `App.jsx` | المكون الرئيسي للتطبيق، إدارة التوجيه (Routing) والحالة العامة (State) |
| `main.jsx` | نقطة دخول التطبيق (React Root) |
| `index.css` | ملف الأنماط الرئيسي |
| `responsive-fixes.css` | تعديلات الأنماط للاستجابة |
| `styles/qr-safe.css` | أنماط خاصة بـ QR |
| `config/` | ملفات التكوين |
| `components/` | مكونات الواجهة الأمامية (UI Components) |
| `core/` | منطق العمل الأساسي (Business Logic) |
| `hooks/` | خطافات React مخصصة (Custom Hooks) |
| `lib/` | مكتبات مساعدة (Utilities) |
| `utils/` | أدوات مساعدة عامة |
| `_archive/` | الملفات القديمة وغير المستخدمة (تم نقلها) |

---

## 3. توثيق مسارات الملفات المستخدمة ومنطقها

### 3.1. ⚙️ ملفات التكوين (Configuration)

| المسار | الوصف |
|--------|--------|
| `frontend/src/config/admin-credentials.js` | يحتوي على دالة `validateAdminCredentials` للتحقق من صلاحيات الإدارة (يستخدم Local Storage للمحاكاة). |

### 3.2. 💻 منطق العمل الأساسي (Core Logic)

| المسار | الوصف |
|--------|--------|
| `frontend/src/core/advanced-queue-engine.js` | منطق متقدم لإدارة الطوابير (Queue Management) |
| `frontend/src/core/event-bus.js` | نظام ناقل الأحداث (Event Bus) للتواصل بين المكونات |
| `frontend/src/core/notification-engine.js` | منطق الإشعارات (Notifications) وتحديث حالة الإشعارات |
| `frontend/src/core/path-engine.js` | منطق المسارات الديناميكية (Dynamic Pathways) |
| `frontend/src/core/pin-engine.js` | منطق نظام PIN (رموز فتح العيادات) |
| `frontend/src/core/queue-engine.js` | منطق الطوابير الأساسي (إضافة، إزالة، حالة) |
| `frontend/src/core/config/refresh.constants.js` | ثوابت خاصة بمعدلات التحديث (Refresh Rates) |

### 3.3. 📚 مكتبات مساعدة (Libraries)

| المسار | الوصف |
|--------|--------|
| `frontend/src/lib/api-unified.js` | **نقطة الوصول الرئيسية للـ API.** يوجه جميع الطلبات إلى `local-api.js` بعد إزالة Supabase. |
| `frontend/src/lib/local-api.js` | **الـ Backend المحاكي.** يحتوي على جميع دوال الـ API التي تستخدم Local Storage لحفظ واسترجاع البيانات (PINs, Queue, Reports, Notifications). |
| `frontend/src/lib/api.js` | (تم استرجاعه) يستخدم كطبقة تجريد للـ API. |
| `frontend/src/lib/auth-service.js` | (تم استرجاعه) يستخدم لخدمات المصادقة الأساسية. |
| `frontend/src/lib/enhanced-api.js` | (تم استرجاعه) طبقة API إضافية للوظائف المتقدمة. |
| `frontend/src/lib/enhanced-themes.js` | منطق الثيمات المتقدمة (Themes) |
| `frontend/src/lib/i18n.js` | نظام الترجمة (Internationalization) |
| `frontend/src/lib/utils.js` | دوال مساعدة عامة (مثل `themes`, `medicalPathways`) |

---

## 4. توثيق واجهات API (Local Storage)

جميع واجهات API الآن هي دوال JavaScript داخل `frontend/src/lib/local-api.js`، وتتفاعل مباشرة مع `localStorage`.

| الدالة (API Endpoint) | الوصف | المكونات المستخدمة |
|-----------------------|--------|--------------------|
| `patientLogin(id, gender)` | تسجيل دخول المريض وحفظ بياناته. | `LoginPage` |
| `enterQueue(clinicId, patientId)` | إدخال المريض إلى طابور العيادة. | `ExamSelectionPage` |
| `queueDone(clinicId, patientId, pin)` | إكمال دور المريض بعد التحقق من PIN. | `AdminPage` |
| `getQueueStatus(clinicId)` | استرجاع حالة الطابور (المنتظرين، الدور الحالي). | `AdminQueueMonitor` |
| `getPinStatus()` | استرجاع قائمة العيادات وأكواد PIN الحالية. | `AdminPINMonitor` |
| `choosePath(gender)` | تحديد المسار الديناميكي للمريض بناءً على الجنس. | `ExamSelectionPage` |
| `addNotification(patientId, msg, type)` | إضافة إشعار جديد للمريض. | `core/notification-engine.js` |
| `getNotifications(patientId)` | استرجاع إشعارات المريض. | `NotificationPanel` |
| `getDailyReport()` | تقرير إحصائي يومي. | `EnhancedAdminDashboard` |
| `getWeeklyReport()` | تقرير إحصائي أسبوعي. | `EnhancedAdminDashboard` |
| `getMonthlyReport()` | تقرير إحصائي شهري. | `EnhancedAdminDashboard` |
| `getAnnualReport()` | تقرير إحصائي سنوي. | `EnhancedAdminDashboard` |
| `getAdminStatus()` | استرجاع حالة الإدارة العامة (إجمالي المرضى، PINs النشطة). | `AdminPage` |

---

## 5. المسارات الديناميكية (Routing)

يعتمد التطبيق على توجيه React الداخلي (Internal React Routing) مع دعم Vercel لـ SPA.

| المسار (URL) | المكون المعروض | الوصف |
|--------------|----------------|--------|
| `/` | `LoginPage` | صفحة تسجيل الدخول الرئيسية. |
| `/login` | `LoginPage` | صفحة تسجيل الدخول الرئيسية. |
| `/exam` | `ExamSelectionPage` | اختيار العيادات والمسار الديناميكي. |
| `/patient` | `PatientPage` | شاشة المريض (عرض حالة الطابور والإشعارات). |
| `/admin` | `AdminPage` | لوحة تحكم الإدارة (PINs, Queue Monitor, Reports). |
| `/qr` | `QrScanPage` | صفحة مسح رمز QR. |
| `/*` | `LoginPage` | توجيه أي مسار غير معروف إلى صفحة تسجيل الدخول. |

---

## 6. الخلاصة

تمت إعادة هيكلة المشروع بنجاح، وتم نقل جميع الملفات غير المستخدمة إلى مجلد `frontend/src/_archive`. المشروع الآن يعمل بالكامل على Local Storage، مما يجعله مثالياً للاختبار والتشغيل المحلي. جميع الوظائف المطلوبة (PIN, Queue, Pathways, Notifications, Reports, Statistics) تعمل بشكل صحيح وتم توثيقها بالكامل.
